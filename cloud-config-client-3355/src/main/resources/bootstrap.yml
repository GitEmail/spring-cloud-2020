server:
  port: 3355

spring:
  application:
    name: cloud-config-client
  cloud:
    config:
      label: master
      name: config
      profile: dev
      uri: http://config-3344:3344
eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.org:7001/eureka